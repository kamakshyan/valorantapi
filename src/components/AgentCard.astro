---
const {namae,imgsrc,role,uuid} = Astro.props;
---

<div class="flex flex-col p-8 items-center border border-black hover:shadow-lg hover:cursor-pointer">
    <img class="w-64" src={imgsrc} alt="">
    <p class="text-xl">{namae}</p>
</div>